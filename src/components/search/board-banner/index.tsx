import { memo } from 'react'

import { BoardBannerWrapper } from './style'

const BoardBanner = memo(() => {
  return (
    <BoardBannerWrapper>BoardBanner</BoardBannerWrapper>
  )
})

export default BoardBanner
